# Office考勤表生成插件 - 技术架构文档

## 1. 项目概述

### 1.1 项目目标
创建一个Office插件，支持通过自然语言描述生成各种格式的考勤表，同时支持Excel和Word。

### 1.2 核心功能
- **自然语言生成**：通过中文描述生成考勤表
- **多格式支持**：日报、周报、月报、汇总表等
- **双平台支持**：Excel和Word
- **图表生成**：自动生成考勤统计图表
- **文件导入**：支持从CSV/Excel文件导入考勤数据

### 1.3 技术选型
| 组件 | 技术 | 说明 |
|------|------|------|
| 前端框架 | React 18 + TypeScript | 组件化开发 |
| UI组件库 | Fluent UI React v9 | 微软官方UI库 |
| Office集成 | Office.js | 官方Add-in API |
| NLP本地 | 规则引擎 + 中文分词 | 离线处理 |
| NLP云端 | OpenAI API | 复杂场景 |
| 图表库 | Chart.js | 统计图表 |
| 构建工具 | Vite | 快速构建 |
| 测试 | Vitest + Testing Library | 单元测试 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Office Application                          │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    Task Pane (插件面板)                       │   │
│  │  ┌─────────────────────────────────────────────────────┐    │   │
│  │  │                  用户界面层 (React)                   │    │   │
│  │  │  ┌───────────┐ ┌───────────┐ ┌───────────┐         │    │   │
│  │  │  │ 输入面板   │ │ 预览面板   │ │ 设置面板   │         │    │   │
│  │  │  └───────────┘ └───────────┘ └───────────┘         │    │   │
│  │  │  ┌───────────┐ ┌───────────┐ ┌───────────┐         │    │   │
│  │  │  │ 模板选择   │ │ 文件导入   │ │ 图表展示   │         │    │   │
│  │  │  └───────────┘ └───────────┘ └───────────┘         │    │   │
│  │  └─────────────────────────────────────────────────────┘    │   │
│  │                            │                                 │   │
│  │  ┌─────────────────────────▼─────────────────────────────┐  │   │
│  │  │                   业务逻辑层                            │  │   │
│  │  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐      │  │   │
│  │  │  │  NLP处理器   │ │  模板引擎   │ │  数据处理器  │      │  │   │
│  │  │  │ (混合方案)   │ │            │ │             │      │  │   │
│  │  │  └─────────────┘ └─────────────┘ └─────────────┘      │  │   │
│  │  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐      │  │   │
│  │  │  │  图表生成器  │ │  文件解析器  │ │  验证器     │      │  │   │
│  │  │  └─────────────┘ └─────────────┘ └─────────────┘      │  │   │
│  │  └─────────────────────────────────────────────────────────┘  │   │
│  │                            │                                 │   │
│  │  ┌─────────────────────────▼─────────────────────────────┐  │   │
│  │  │                  Office.js 适配层                       │  │   │
│  │  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐      │  │   │
│  │  │  │ Excel适配器  │ │ Word适配器   │ │ 通用适配器   │      │  │   │
│  │  │  └─────────────┘ └─────────────┘ └─────────────┘      │  │   │
│  │  └─────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                            │                                        │
│  ┌─────────────────────────▼─────────────────────────────────────┐ │
│  │              Office Document (Excel/Word)                      │ │
│  └────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      外部服务 (可选)                                 │
│  ┌─────────────────┐                                               │
│  │   OpenAI API    │  ← 复杂NLP场景时调用                           │
│  └─────────────────┘                                               │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责

#### 2.2.1 用户界面层
- **输入面板**：自然语言输入框，支持语音输入（可选）
- **预览面板**：实时预览生成的考勤表
- **设置面板**：API配置、默认模板、样式设置
- **模板选择**：预设模板库浏览和选择
- **文件导入**：CSV/Excel文件上传和解析
- **图表展示**：考勤统计图表显示

#### 2.2.2 业务逻辑层
- **NLP处理器**：解析自然语言，提取考勤表参数
- **模板引擎**：管理和渲染考勤表模板
- **数据处理器**：处理考勤数据计算和转换
- **图表生成器**：生成统计图表
- **文件解析器**：解析导入的文件
- **验证器**：数据验证和错误处理

#### 2.2.3 Office.js适配层
- **Excel适配器**：Excel特定操作（单元格、公式、图表）
- **Word适配器**：Word特定操作（表格、样式）
- **通用适配器**：共享功能（文档信息、用户设置）

---

## 3. 核心模块设计

### 3.1 自然语言处理模块 (NLP)

#### 3.1.1 混合处理流程

```
用户输入
    │
    ▼
┌─────────────────┐
│   预处理器       │  ← 清理、分词、标准化
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  本地规则引擎    │────▶│  置信度评估      │
└────────┬────────┘     └────────┬────────┘
         │                       │
         │              置信度 < 阈值?
         │                       │
         │         是            │ 否
         │    ┌──────────────────┘
         │    │
         │    ▼
         │  ┌─────────────────┐
         │  │   OpenAI API    │
         │  └────────┬────────┘
         │           │
         ▼           ▼
┌─────────────────────────────┐
│      结果合并与验证          │
└─────────────────────────────┘
         │
         ▼
    结构化输出
```

#### 3.1.2 本地规则引擎设计

```typescript
// 意图识别规则
interface IntentRule {
  patterns: RegExp[];
  intent: AttendanceIntent;
  priority: number;
}

// 实体提取规则
interface EntityRule {
  type: EntityType;
  patterns: RegExp[];
  extractor: (match: RegExpMatchArray) => any;
}

// 支持的意图类型
enum AttendanceIntent {
  CREATE_DAILY = 'create_daily',      // 创建日考勤
  CREATE_WEEKLY = 'create_weekly',    // 创建周考勤
  CREATE_MONTHLY = 'create_monthly',  // 创建月考勤
  CREATE_SUMMARY = 'create_summary',  // 创建汇总表
  IMPORT_DATA = 'import_data',        // 导入数据
  GENERATE_CHART = 'generate_chart',  // 生成图表
}

// 实体类型
enum EntityType {
  DATE_RANGE = 'date_range',
  EMPLOYEE_LIST = 'employee_list',
  DEPARTMENT = 'department',
  COLUMNS = 'columns',
  STATISTICS = 'statistics',
}
```

#### 3.1.3 NLP输出格式

```typescript
interface NLPResult {
  intent: AttendanceIntent;
  confidence: number;
  entities: {
    dateRange?: {
      start: Date;
      end: Date;
    };
    employees?: string[];
    department?: string;
    columns?: ColumnType[];
    statistics?: StatisticType[];
    format?: 'excel' | 'word';
  };
  rawInput: string;
  processedBy: 'local' | 'api' | 'hybrid';
}
```

### 3.2 模板引擎模块

#### 3.2.1 模板类型定义

```typescript
interface AttendanceTemplate {
  id: string;
  name: string;
  type: TemplateType;
  description: string;
  
  // 结构定义
  structure: {
    headers: HeaderDefinition[];
    rows: RowDefinition;
    footer?: FooterDefinition;
  };
  
  // 样式定义
  styles: {
    headerStyle: CellStyle;
    dataStyle: CellStyle;
    alternateRowStyle?: CellStyle;
    summaryStyle?: CellStyle;
  };
  
  // 公式定义（Excel专用）
  formulas?: FormulaDefinition[];
  
  // 条件格式
  conditionalFormats?: ConditionalFormat[];
}

enum TemplateType {
  DAILY_SIMPLE = 'daily_simple',           // 简单日报
  DAILY_DETAILED = 'daily_detailed',       // 详细日报
  WEEKLY_STANDARD = 'weekly_standard',     // 标准周报
  MONTHLY_STANDARD = 'monthly_standard',   // 标准月报
  MONTHLY_SUMMARY = 'monthly_summary',     // 月度汇总
  CUSTOM = 'custom',                       // 自定义
}
```

#### 3.2.2 预设模板库

```
templates/
├── daily/
│   ├── simple.json          # 简单打卡表
│   └── detailed.json        # 详细工时表
├── weekly/
│   ├── standard.json        # 标准周报
│   └── flex-time.json       # 弹性工时表
├── monthly/
│   ├── standard.json        # 标准月报
│   ├── summary.json         # 汇总统计表
│   └── department.json      # 部门考勤表
└── custom/
    └── user-templates/      # 用户自定义模板
```

### 3.3 数据模型

#### 3.3.1 核心数据结构

```typescript
// 员工信息
interface Employee {
  id: string;
  name: string;
  department?: string;
  position?: string;
}

// 考勤记录
interface AttendanceRecord {
  employeeId: string;
  date: Date;
  checkIn?: Date;
  checkOut?: Date;
  status: AttendanceStatus;
  workHours?: number;
  overtime?: number;
  notes?: string;
}

// 考勤状态
enum AttendanceStatus {
  PRESENT = 'present',       // 出勤
  ABSENT = 'absent',         // 缺勤
  LATE = 'late',             // 迟到
  EARLY_LEAVE = 'early',     // 早退
  LEAVE = 'leave',           // 请假
  HOLIDAY = 'holiday',       // 节假日
  WEEKEND = 'weekend',       // 周末
}

// 考勤表数据
interface AttendanceSheet {
  id: string;
  title: string;
  type: TemplateType;
  dateRange: {
    start: Date;
    end: Date;
  };
  employees: Employee[];
  records: AttendanceRecord[];
  statistics?: AttendanceStatistics;
  createdAt: Date;
  updatedAt: Date;
}

// 统计数据
interface AttendanceStatistics {
  totalDays: number;
  workDays: number;
  byEmployee: Map<string, EmployeeStatistics>;
  summary: {
    totalPresent: number;
    totalAbsent: number;
    totalLate: number;
    totalEarlyLeave: number;
    totalOvertime: number;
  };
}
```

### 3.4 Office适配器设计

#### 3.4.1 适配器接口

```typescript
interface OfficeAdapter {
  // 文档操作
  createDocument(): Promise<void>;
  getActiveDocument(): Promise<DocumentInfo>;
  
  // 表格操作
  createTable(config: TableConfig): Promise<TableReference>;
  updateTable(ref: TableReference, data: any[][]): Promise<void>;
  applyStyles(ref: TableReference, styles: StyleConfig): Promise<void>;
  
  // 图表操作（Excel专用）
  createChart?(config: ChartConfig): Promise<ChartReference>;
  
  // 导出操作
  exportToPDF?(): Promise<Blob>;
}

// Excel适配器
class ExcelAdapter implements OfficeAdapter {
  async createTable(config: TableConfig): Promise<TableReference> {
    return Excel.run(async (context) => {
      const sheet = context.workbook.worksheets.getActiveWorksheet();
      const range = sheet.getRange(config.range);
      
      // 设置数据
      range.values = config.data;
      
      // 创建表格
      const table = sheet.tables.add(range, config.hasHeaders);
      table.name = config.name;
      
      // 应用样式
      if (config.style) {
        table.style = config.style;
      }
      
      await context.sync();
      return { id: table.id, name: table.name };
    });
  }
  
  async createChart(config: ChartConfig): Promise<ChartReference> {
    return Excel.run(async (context) => {
      const sheet = context.workbook.worksheets.getActiveWorksheet();
      const chart = sheet.charts.add(
        config.type,
        sheet.getRange(config.dataRange),
        config.seriesBy
      );
      
      chart.title.text = config.title;
      chart.setPosition(config.position.top, config.position.left);
      
      await context.sync();
      return { id: chart.id };
    });
  }
}

// Word适配器
class WordAdapter implements OfficeAdapter {
  async createTable(config: TableConfig): Promise<TableReference> {
    return Word.run(async (context) => {
      const body = context.document.body;
      
      // 创建表格
      const table = body.insertTable(
        config.data.length,
        config.data[0].length,
        Word.InsertLocation.end,
        config.data
      );
      
      // 应用样式
      if (config.style) {
        table.styleBuiltIn = config.style;
      }
      
      await context.sync();
      return { id: 'word-table' };
    });
  }
}
```

### 3.5 文件导入模块

#### 3.5.1 支持的文件格式

```typescript
enum ImportFileType {
  CSV = 'csv',
  XLSX = 'xlsx',
  XLS = 'xls',
}

interface ImportConfig {
  fileType: ImportFileType;
  hasHeader: boolean;
  columnMapping: ColumnMapping[];
  dateFormat: string;
  encoding?: string;  // CSV专用
}

interface ColumnMapping {
  sourceColumn: number | string;
  targetField: keyof AttendanceRecord;
  transform?: (value: any) => any;
}
```

#### 3.5.2 导入流程

```
文件选择
    │
    ▼
┌─────────────────┐
│   文件类型检测   │
└────────┬────────┘
         │
    ┌────┴────┬────────┐
    ▼         ▼        ▼
┌───────┐ ┌───────┐ ┌───────┐
│ CSV   │ │ XLSX  │ │ XLS   │
│ 解析器 │ │ 解析器 │ │ 解析器 │
└───┬───┘ └───┬───┘ └───┬───┘
    │         │        │
    └────┬────┴────────┘
         │
         ▼
┌─────────────────┐
│   数据预览       │  ← 用户确认列映射
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   数据验证       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   数据转换       │
└────────┬────────┘
         │
         ▼
    导入完成
```

### 3.6 图表生成模块

#### 3.6.1 支持的图表类型

```typescript
enum ChartType {
  // 出勤统计
  ATTENDANCE_PIE = 'attendance_pie',           // 出勤率饼图
  ATTENDANCE_BAR = 'attendance_bar',           // 出勤柱状图
  
  // 趋势分析
  DAILY_TREND = 'daily_trend',                 // 每日趋势
  MONTHLY_TREND = 'monthly_trend',             // 月度趋势
  
  // 对比分析
  EMPLOYEE_COMPARISON = 'employee_comparison', // 员工对比
  DEPARTMENT_COMPARISON = 'dept_comparison',   // 部门对比
  
  // 工时统计
  WORK_HOURS_BAR = 'work_hours_bar',          // 工时柱状图
  OVERTIME_LINE = 'overtime_line',             // 加班趋势
}

interface ChartConfig {
  type: ChartType;
  title: string;
  dataRange: string;
  seriesBy: 'rows' | 'columns';
  position: {
    top: string;
    left: string;
    width?: number;
    height?: number;
  };
  options?: {
    showLegend?: boolean;
    showDataLabels?: boolean;
    colors?: string[];
  };
}
```

---

## 4. 项目结构

```
attendance-office-addin/
├── manifest.xml                    # Office Add-in 清单
├── package.json
├── tsconfig.json
├── vite.config.ts
├── .env.example                    # 环境变量示例
│
├── src/
│   ├── main.tsx                    # 应用入口
│   ├── App.tsx                     # 根组件
│   │
│   ├── components/                 # UI组件
│   │   ├── common/                 # 通用组件
│   │   │   ├── Button/
│   │   │   ├── Input/
│   │   │   ├── Modal/
│   │   │   └── Loading/
│   │   │
│   │   ├── panels/                 # 面板组件
│   │   │   ├── InputPanel/         # 自然语言输入
│   │   │   ├── PreviewPanel/       # 预览面板
│   │   │   ├── TemplatePanel/      # 模板选择
│   │   │   ├── ImportPanel/        # 文件导入
│   │   │   ├── ChartPanel/         # 图表展示
│   │   │   └── SettingsPanel/      # 设置面板
│   │   │
│   │   └── attendance/             # 考勤相关组件
│   │       ├── AttendanceTable/
│   │       ├── AttendanceChart/
│   │       └── AttendanceStats/
│   │
│   ├── core/                       # 核心业务逻辑
│   │   ├── nlp/                    # NLP模块
│   │   │   ├── index.ts
│   │   │   ├── processor.ts        # NLP处理器
│   │   │   ├── rules/              # 规则定义
│   │   │   │   ├── intents.ts
│   │   │   │   ├── entities.ts
│   │   │   │   └── patterns.ts
│   │   │   ├── tokenizer.ts        # 分词器
│   │   │   └── api-client.ts       # OpenAI客户端
│   │   │
│   │   ├── templates/              # 模板引擎
│   │   │   ├── index.ts
│   │   │   ├── engine.ts           # 模板引擎
│   │   │   ├── renderer.ts         # 渲染器
│   │   │   └── presets/            # 预设模板
│   │   │       ├── daily.ts
│   │   │       ├── weekly.ts
│   │   │       └── monthly.ts
│   │   │
│   │   ├── generator/              # 生成器
│   │   │   ├── index.ts
│   │   │   ├── attendance-generator.ts
│   │   │   ├── chart-generator.ts
│   │   │   └── style-manager.ts
│   │   │
│   │   └── data/                   # 数据处理
│   │       ├── index.ts
│   │       ├── processor.ts
│   │       ├── validator.ts
│   │       └── statistics.ts
│   │
│   ├── adapters/                   # Office适配器
│   │   ├── index.ts
│   │   ├── base.ts                 # 基础适配器
│   │   ├── excel-adapter.ts        # Excel适配器
│   │   ├── word-adapter.ts         # Word适配器
│   │   └── utils.ts                # 工具函数
│   │
│   ├── services/                   # 服务层
│   │   ├── import-service.ts       # 文件导入服务
│   │   ├── export-service.ts       # 导出服务
│   │   ├── storage-service.ts      # 本地存储
│   │   └── config-service.ts       # 配置管理
│   │
│   ├── hooks/                      # React Hooks
│   │   ├── useOffice.ts
│   │   ├── useNLP.ts
│   │   ├── useTemplate.ts
│   │   └── useImport.ts
│   │
│   ├── store/                      # 状态管理
│   │   ├── index.ts
│   │   ├── attendance-store.ts
│   │   ├── template-store.ts
│   │   └── settings-store.ts
│   │
│   ├── types/                      # 类型定义
│   │   ├── attendance.ts
│   │   ├── template.ts
│   │   ├── nlp.ts
│   │   └── office.ts
│   │
│   ├── utils/                      # 工具函数
│   │   ├── date.ts
│   │   ├── format.ts
│   │   ├── validation.ts
│   │   └── file.ts
│   │
│   └── styles/                     # 样式文件
│       ├── global.css
│       ├── variables.css
│       └── components/
│
├── public/                         # 静态资源
│   ├── icons/
│   │   ├── icon-16.png
│   │   ├── icon-32.png
│   │   ├── icon-80.png
│   │   └── icon-128.png
│   └── templates/                  # 模板JSON文件
│
├── tests/                          # 测试文件
│   ├── unit/
│   │   ├── nlp/
│   │   ├── templates/
│   │   └── adapters/
│   └── integration/
│
└── docs/                           # 文档
    ├── ARCHITECTURE.md             # 架构文档（本文件）
    ├── API.md                      # API文档
    ├── USER_GUIDE.md               # 用户指南
    └── DEVELOPMENT.md              # 开发指南
```

---

## 5. 接口设计

### 5.1 NLP处理器接口

```typescript
interface INLPProcessor {
  // 处理自然语言输入
  process(input: string): Promise<NLPResult>;
  
  // 设置处理模式
  setMode(mode: 'local' | 'api' | 'hybrid'): void;
  
  // 设置置信度阈值
  setConfidenceThreshold(threshold: number): void;
  
  // 添加自定义规则
  addCustomRule(rule: IntentRule | EntityRule): void;
}
```

### 5.2 模板引擎接口

```typescript
interface ITemplateEngine {
  // 获取所有模板
  getTemplates(): AttendanceTemplate[];
  
  // 获取指定模板
  getTemplate(id: string): AttendanceTemplate | null;
  
  // 渲染模板
  render(template: AttendanceTemplate, data: AttendanceSheet): RenderedTable;
  
  // 保存自定义模板
  saveCustomTemplate(template: AttendanceTemplate): Promise<void>;
  
  // 删除自定义模板
  deleteCustomTemplate(id: string): Promise<void>;
}
```

### 5.3 生成器接口

```typescript
interface IAttendanceGenerator {
  // 从NLP结果生成考勤表
  generateFromNLP(result: NLPResult): Promise<AttendanceSheet>;
  
  // 从模板生成考勤表
  generateFromTemplate(
    template: AttendanceTemplate,
    params: GenerationParams
  ): Promise<AttendanceSheet>;
  
  // 从导入数据生成考勤表
  generateFromImport(
    data: ImportedData,
    template?: AttendanceTemplate
  ): Promise<AttendanceSheet>;
  
  // 输出到Office文档
  outputToDocument(
    sheet: AttendanceSheet,
    adapter: OfficeAdapter
  ): Promise<void>;
}
```

---

## 6. 用户界面设计

### 6.1 主界面布局

```
┌─────────────────────────────────────────┐
│  📊 考勤表生成器                    ⚙️  │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 请描述您需要的考勤表...          │   │
│  │                                 │   │
│  │ 例如：生成2024年1月的月度考勤表  │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [🎤 语音] [📁 导入文件] [📋 选择模板]  │
│                                         │
├─────────────────────────────────────────┤
│  快速模板：                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐      │
│  │日报 │ │周报 │ │月报 │ │汇总 │      │
│  └─────┘ └─────┘ └─────┘ └─────┘      │
├─────────────────────────────────────────┤
│  预览：                                 │
│  ┌─────────────────────────────────┐   │
│  │  姓名 │ 1  │ 2  │ 3  │...│ 统计 │   │
│  │───────┼────┼────┼────┼───┼──────│   │
│  │  张三 │ ✓  │ ✓  │ 休 │...│  22  │   │
│  │  李四 │ ✓  │ 迟 │ 休 │...│  21  │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [📊 生成图表]  [✅ 生成到文档]         │
│                                         │
└─────────────────────────────────────